-- gObl00x Notification
game:GetService("StarterGui"):SetCore("SendNotification", { 
	Title = "gOb scripts";
	Text = "LOL Time to Exploit!";
	Icon = "rbxthumb://type=Asset&id=126389658690593&w=150&h=150"})
Duration = 15;

-- Dance 1 (The Hero)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "The Hero"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("The Hero.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/The%20Hero.mp3"))     
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 133160365635320)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("The Hero.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 1 and music stopped")
end)



-- Dance 2 (Two)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Two"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Two.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Two.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 137845929482571)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Two.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 2 and music stopped")
end)



-- Dance 3 (The Flop)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "The Flop"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("The Flop.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/The%20Flop.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 84447598378239)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("The Flop.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 3 and music stopped")
end)



-- Dance 4 (Club Penguin)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Club Penguin"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Club Penguin.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Club%20Penguin.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 89761302048916)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Club Penguin.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 4 and music stopped")
end)



-- Dance 5 (Breakdance)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Breakdance"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Breakdance.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Breakdance.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 132886479585903)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Breakdance.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 5 and music stopped")
end)



-- Dance 6 (Insanity)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Insanity"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Insanity.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Insanity.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 139483347792972)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Insanity.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 6 and music stopped")
end)



-- Dance 7 (Mannrobics)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Mannrobics"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Mannrobics.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Mannrobics.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 137456359844967)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Mannrobics.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 7 and music stopped")
end)



-- Dance 8 (Step)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Step"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Step.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Step.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 124074233795203)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Step.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
    
    player.Character.Humanoid.WalkSpeed = 3
end)

tool.Unequipped:Connect(function()
    player.Character.Humanoid.WalkSpeed = 16
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 8 and music stopped")
end)



-- Dance 9 (Poke Dance)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Poke Dance"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Poke Dance.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Poke%20Dance.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 18986687692)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Poke Dance.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 9 and music stopped")
end)



-- Dance 10 (Spooky Month)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Spooky Month"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Spooky Month.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Spooky%20Month.mp3"))
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 15231364673)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Spooky Month.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 10 and music stopped")
end)



-- Dance 11 (Chinese Dance)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Chinese Dance"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Chinese Dance.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Chinese%20Dance.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 124210157097622)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Chinese Dance.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 11 and music stopped")
end)



-- Dance 12 (Doodle)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Doodle"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Doodle.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Doodle.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 137721173051346)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Doodle.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 12 and music stopped")
end)



-- Dance 13 (Shinji Chair)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Shinji Chair"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Shinji Meme.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Shinji%20Meme.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 130485792890829)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Shinji Meme.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 13 and music stopped")
end)



-- Dance 14 (Rakuten Lemonade)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Rakuten Lemonade"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Rakuten Lemonade.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Rakuten%20Lemonade.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 18985726113)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Rakuten Lemonade.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 14 and music stopped")
end)



-- Dance 15 (Lonely Lonely)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Lonely Lonely"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Lonely Lonely.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Lonely%20Lonely.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 135780829162802)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Lonely Lonely.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end

    player.Character.Humanoid.WalkSpeed = 10

end)

tool.Unequipped:Connect(function()
    player.Character.Humanoid.WalkSpeed = 16
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 15 and music stopped")
end)



-- Dance 16 (Boppin)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Boppin"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Boppin.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Boppin.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 13579968035)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Boppin.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 16 and music stopped")
end)



-- Dance 17 (Shuba Duck)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Shuba Duck"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Hey Ya.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Hey%20Ya.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 13357063395)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Hey Ya.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 17 and music stopped")
end)



-- Dance 18 (Goopie)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Goopie"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Goopie.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Goopie.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 7828640127)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Goopie.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 18 and music stopped")
end)



-- Dance 19 (Prince of Egypt)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Prince of Egypt"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Prince Of Egypt.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Prince%20Of%20Egypt.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 100136360352723)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Prince Of Egypt.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 19 and music stopped")
end)



-- Dance 20 (WMD Boombox)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "WMD Boombox"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Wipe me Down.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Wipe%20me%20Down.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 72460213504303)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Wipe me Down.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 20 and music stopped")
end)



-- Dance 21 (Shoo Whop)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Shoo Whop"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Shoo Whop.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Shoo%20Whop.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 121535445943256)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Shoo Whop.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 21 and music stopped")
end)


-- Dance 22 (PoPiPo) 
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "PoPiPo"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("PoPiPo.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/PoPiPo.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 78991327797272)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("PoPiPo.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 22 and music stopped")
end)


-- Dance 23 (Monoko) 
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Monoko"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Monoko.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Monoko.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 139889845987864)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Monoko.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end

    player.Character.Humanoid.WalkSpeed = 8

end)

tool.Unequipped:Connect(function()
    player.Character.Humanoid.WalkSpeed = 16
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 23 and music stopped")
end)


-- Dance 24 (Lagtrain) 
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Lagtrain"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Lagtrain.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Lagtrain.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 131559207454945)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Lagtrain.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end

    player.Character.Humanoid.WalkSpeed = 20

end)

tool.Unequipped:Connect(function()
    player.Character.Humanoid.WalkSpeed = 16
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 24 and music stopped")
end)


-- Dance 25 (Inside In My Head)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Inside In My Head"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("In My Head.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/In%20My%20Head.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 114160464579438)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("In My Head.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 25 and music stopped")
end)


-- Dance 26 (Lemon Melon Cookie)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Lemon Melon Cookie"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("LMC.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/LMC.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 120262284704633)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("LMC.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 26 and music stopped")
end)


-- Dance 27 (Get Down)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Get Down"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Get Down.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Get%20Down.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 98916367562022)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Get Down.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 27 and music stopped")
end)


-- Dance 28 (Dia Delicia)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Dia Delicia"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Dia Delicia.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Dia%20Delicia.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 80641587562132)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Dia Delicia.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 28 and music stopped")
end)


-- Dance 29 (Epik)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Epik"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Epik.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Epik.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 135424282094138)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Epik.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 29 and music stopped")
end)


-- Dance 30 (Shigure)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Shigure"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Loli Dance.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Loli%20Dance.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 14887006269)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Loli Dance.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 30 and music stopped")
end)


-- Dance 31 (I WANNA RUN AWAY)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "I WANNA RUN AWAY🗣️"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("IWRA.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/IWRA.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 131562546189485)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("IWRA.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 31 and music stopped")
end)


-- Dance 32 (WhatsApp Meme)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "WhatsApp Meme"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("WhatsApp Meme.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/WhatsApp%20Meme.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 131539514978219)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("WhatsApp Meme.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 32 and music stopped")
end)


-- Dance 33 (Goofy)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Goofy"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Goofy Song.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Goofy%20Song.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 75148929064618)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Goofy Song.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 33 and music stopped")
end)


-- Dance 34 (Who Can It Be Now?)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Who Can It Be Now?"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("WCIBN.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/WCIBN.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 16240038168)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("WCIBN.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 34 and music stopped")
end)


-- Dance 35 (Pretty Princess)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Pretty Princess"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Pretty Princess Theme.mp3", game:HttpGet("https://github.com/gObl00x/Epik-Musics/raw/refs/heads/main/Pretty%20Princess%20Theme.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 118452043589079)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Pretty Princess Theme.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 35 and music stopped")
end)


-- Dance 36 (I Miss The Quiet)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "I Miss The Quiet"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("IMissTheQuietYesIdooo.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/IMissTheQuietYesIdooo.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 100986631322204)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("IMissTheQuietYesIdooo.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 36 and music stopped")
end)


-- Dance 37 (Brickbattle)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Brickbattle"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Brickbattler.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Brickbattler.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 138531569676600)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Brickbattler.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 37 and music stopped")
end)


-- Dance 38 (Cupcakes)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Pretty Princess"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Cupcakes HD.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Cupcakes%20HD.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 126397719425664)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Cupcakes HD.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 38 and music stopped")
end)


-- Dance 39 (2two2)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "2Two2"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("2Two2.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/2Two2.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 89348834081653)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("2Two2.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 39 and music stopped")
end)


-- Dance 40 (Arona Dance)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Arona Dance"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("AronaDance.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/AronaDance.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 113953995772094)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("AronaDance.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 40 and music stopped")
end)


-- Dance 41 (Assumptions)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Assumptions ReDance"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Assumptions.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Assumptions.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 75148929064618)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Assumptions.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 41 and music stopped")
end)


-- Dance 42 (BIGSHOT)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "BIGSHOT"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("BIGSHOT.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/BIGSHOT.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 8071025449)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("BIGSHOT.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 42 and music stopped")
end)


-- Dance 43 (Spamtom Dance)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Spamtom Dance"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("BIGSHOT.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/BIGSHOT.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 89348834081653)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("BIGSHOT.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 43 and music stopped")
end)


-- Dance 44 (Bluudud Alarm)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Bluudud Alarm"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Bluudud Alarm.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Bluudud%20Alarm.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 97789180606119)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Bluudud Alarm.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 44 and music stopped")
end)


-- Dance 45 (C14)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "C14"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("C14.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/C14.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 118452043589079)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("C14.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 45 and music stopped")
end)


-- Dance 46 (Cafeteria)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Cafeteria"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Cafeteria.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Cafeteria.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 111079857314277)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Cafeteria.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 46 and music stopped")
end)


-- Dance 47 (Sit)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Sit"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Calm.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Calm.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 134311400337263)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Calm.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 47 and music stopped")
end)


-- Dance 48 (Dance is you are peak)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Dance if You Are Peak"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Dance is you are peak (Headlock).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Dance%20is%20you%20are%20peak%20(Headlock).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 135424282094138)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Dance is you are peak (Headlock).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 48 and music stopped")
end)


-- Dance 49 (Endoskeleton)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "EndoSkeleton Dance"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Endoskeleton.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Endoskeleton.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 99015977795000)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Endoskeleton.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 49 and music stopped")
end)


-- Dance 50 (Epical)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Epical"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Epical.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Epical.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 135424282094138)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Epical.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 50 and music stopped")
end)


-- Dance 51 (GET DOWN!)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Get Down!"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("GET DOWN!2.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/GET%20DOWN!2.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 98916367562022)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("GET DOWN!2.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 51 and music stopped")
end)


-- Dance 52 (Internet Angel)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Internet Angel"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("INTERNET ANGEL.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/INTERNET%20ANGEL.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 18986591475)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("INTERNET ANGEL.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 52 and music stopped")
end)


-- Dance 53 (Internet Yamero)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Internet Yamero"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Internet Yamero.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/InternetYamero.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 99905571448278)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("InternetYamero.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 53 and music stopped")
end)


-- Dance 54 (Iron Lung)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Iron Lung"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Ironlung.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Ironlung.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 77298361270388)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Ironlung.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 54 and music stopped")
end)


-- Dance 55 (Tenna)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Tenna"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("ItsTVTIME!.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/ItsTVTIME!.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 97351316825183)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("ItsTVTIME!.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 55 and music stopped")
end)


-- Dance 56 (Konton)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Konton"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Konton.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Konton.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 83719399570827)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Konton.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 56 and music stopped")
end)


-- Dance 57 (Lemon Demon)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Lemon Demon"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Lemon Demon - Modify.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Lemon%20Demon%20-%20Modify.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 101484588555482)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Lemon Demon - Modify.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 57 and music stopped")
end)


-- Dance 58 (Liar Dance)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Liar Dance"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("LiarDance.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/LiarDance.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 129584064125006)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("LiarDance.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 58 and music stopped")
end)


-- Dance 59 (Limbo)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Limbo"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Limbo.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Limbo.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 17308598027)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Limbo.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 59 and music stopped")
end)


-- Dance 60 (Shucks Mid Customscene)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Shucks Mid Customscene"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("MID SHUCKS SPANISH.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/MID%20SHUCKS%20SPANISH.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 129311159923059)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("MID SHUCKS SPANISH.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 60 and music stopped")
end)


-- Dance 61 (Mesmerizer)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Mesmerizer"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Mesmerizer.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Mesmerizer.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 107578737342278)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Mesmerizer.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 61 and music stopped")
end)


-- Dance 62 (Out of touch)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Out of touch"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Out of touch.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Out%20of%20touch.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 135867676375598)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Out of touch.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 62 and music stopped")
end)


-- Dance 63 (RUNAWAY)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "RUN AWAY"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("RUNAWAY.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/RUNAWAY.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 119147810948063)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("RUNAWAY.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 63 and music stopped")
end)


-- Dance 64 (Shucks)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Shucks"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Shucks.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Shucks.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 74238051754912)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Shucks.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 64 and music stopped")
end)


-- Dance 65 (Jun)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Jun fight with a paralysis demon"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Shinji's and juns Final battle Song.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Shinji's%20and%20juns%20Final%20battle%20Song.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 108034281000187)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Shinji's and juns Final battle Song.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 65 and music stopped")
end)


-- Dance 66 (Break core)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Let me break down for you mark (Sinful)"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Sinful.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Sinful.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 132886479585903)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Sinful.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 66 and music stopped")
end)


-- Dance 67 (Silly Billy)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Silly Billy"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("SillyBilly.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/SillyBilly.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 107464355830477)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("SillyBilly.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 67 and music stopped")
end)


-- Dance 68 (Static)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Panic, In Static!"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Static2.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Static2.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 124415802608501)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Static2.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 68 and music stopped")
end)


-- Dance 69 (Subterfuge)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Subterfuge"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Subturefuge.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Subturefuge.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 87482480949358)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Subturefuge.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 69 and music stopped")
end)


-- Dance 70 (Muffin)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Die die die!, Its muffin time!"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("THEMUFFINSONG.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/THEMUFFINSONG.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 70924317602904)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("THEMUFFINSONG.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 70 and music stopped")
end)


-- Dance 71 (TWERKNATION28-NUMBER)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "TWERKNATION28-NUMBER"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("TWERKNATION28-NUMBER.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/TWERKNATION28-NUMBER.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 131539514978219)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("TWERKNATION28-NUMBER.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 71 and music stopped")
end)


-- Dance 72 (Twiddlefinger)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Twiddlefinger"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("TWIDDLEFINGER.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/TWIDDLEFINGER.mp3.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 18376750786)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("TWIDDLEFINGER.mp3.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 72 and music stopped")
end)


-- Dance 73 (Tick tock)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Tick tock!"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("TickTockEmote.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/TickTockEmote.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 74187447524398)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("TickTockEmote.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 73 and music stopped")
end)


-- Dance 74 (Teto)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Teto"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("おちゃめ機能.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/おちゃめ機能.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 95648759792903)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("おちゃめ機能.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 74 and music stopped")
end)


-- Dance 75 (Chronoshift)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Chronoshift"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("chronoshift.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/chronoshift.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 136302258505787)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("chronoshift.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 75 and music stopped")
end)


-- Dance 76 (Noli Intro)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Noli Intro"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("NOLI.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/NOLI.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 84987078674309)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("NOLI.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 76 and music stopped")
end)


-- Dance 77 (Noli Outro)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Noli Outro"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("NOLIOutro.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/NOLIOutro.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 134750797885169)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("NOLIOutro.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 77 and music stopped")
end)


-- Dance 78 (Noli Stund)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Noli Stun (Pose)"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("VOIDSOUND.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/VOIDSOUND.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 137680069002162)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("VOIDSOUND.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 78 and music stopped")
end)


-- Dance 79 (Saxophone)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "I got a brand new saxaphone"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("SAXAPHONE.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/SAXAPHONE.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 76640626326290)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("SAXAPHONE.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 79 and music stopped")
end)


-- Dance 80 (Eggroll)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "EggRoll"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("Eggroll.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/Eggroll.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 108580088290625)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("Eggroll.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 80 and music stopped")
end)


-- Dance 81 (So much)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "So much (I dont know the name)"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 90600572265074)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 81 and music stopped")
end)




-- Dance 82 (Skeleton)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Skeleton (I dont know the name)"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 103054119684229)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 82 and music stopped")
end)


-- Dance 83 (Bang up)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Bang up (I dont know the name)"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 70974671275509)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 83 and music stopped")
end)


-- Dance 84 (Winner)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Winner"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 83803912096445)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 84 and music stopped")
end)


-- Dance 85 (Silly)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Silly (I dont know the name)"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 118335464128228)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 85 and music stopped")
end)


-- Dance 86 (Smg3's MEME video)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Smg3's MEME video"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 131349023319484)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 86 and music stopped")
end)


-- Dance 87 (PP Music)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "PP Music"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 133161918594121)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 87 and music stopped")
end)


-- Dance 88 (Headless)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Headless"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 103625946247167)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 88 and music stopped")
end)


-- Dance 89 (Spin)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Spin"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 93325509946296)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 89 and music stopped")
end)


-- Dance 90 (Epic Backflips)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Epic Backflips"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 98169004962490)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 90 and music stopped")
end)


-- Dance 91 (Rare head movement)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Rare head movement"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 84446932319032)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 91 and music stopped")
end)


-- Dance 92 (PARROT PARTAYYYYYYYYYYY poggers)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "PARROT PARTAYYYYYYYYYYY poggers"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 121135995471744)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 92 and music stopped")
end)


-- Dance 93 (JumpStyle)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "JumpStyle"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 101682333769403)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 93 and music stopped")
end)


-- Dance 94 (freeter dance)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "freeter dance"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 85937050685664)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 94 and music stopped")
end)


-- Dance 95 (inte)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "inte"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 139219469387832)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 95 and music stopped")
end)


-- Dance 96 (Epic Backflips)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Epic Backflips"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 98169004962490)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 96 and music stopped")
end)


-- Dance 97 (Epic Backflips)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Epic Backflips"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 98169004962490)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 97 and music stopped")
end)


-- Dance 98 (Run) 
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Run"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 81655662049376)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end

    player.Character.Humanoid.WalkSpeed = 20

end)

tool.Unequipped:Connect(function()
    player.Character.Humanoid.WalkSpeed = 16
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 98 and music stopped")
end)


-- Dance 99 (Another Run) 
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Another Run"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 127967668233985)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end

    player.Character.Humanoid.WalkSpeed = 25

end)

tool.Unequipped:Connect(function()
    player.Character.Humanoid.WalkSpeed = 16
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 99 and music stopped")
end)


-- Dance 100 (Another Run) 
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Another Run"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 119558526211035)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end

    player.Character.Humanoid.WalkSpeed = 30

end)

tool.Unequipped:Connect(function()
    player.Character.Humanoid.WalkSpeed = 16
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 100 and music stopped")
end)


-- Dance 101 (Desanimado)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Desanimado"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("VOIDSOUND.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/VOIDSOUND.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 126302510476022)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("VOIDSOUND.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 101 and music stopped")
end)


-- Dance 102 (Panic)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "PanicPanicPanicPanic"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("VOIDSOUND.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/VOIDSOUND.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 99927783347758)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("VOIDSOUND.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 102 and music stopped")
end)


-- Dance 103 (Panic Failed)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Panic Failed"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("VOIDSOUND.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/VOIDSOUND.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 98836175112219)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("VOIDSOUND.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 103 and music stopped")
end)


-- Dance 104 (Sorpresa)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Sorpresa"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("VOIDSOUND.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/VOIDSOUND.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 78544606501153)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("VOIDSOUND.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 104 and music stopped")
end)


-- Dance 105 (Rodillas)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Rodillas"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("VOIDSOUND.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/VOIDSOUND.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 108124040461994)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("VOIDSOUND.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 105 and music stopped")
end)

































-- Dance 106 (Death)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Death"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("VOIDSOUND.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/VOIDSOUND.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 96550243316072)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("VOIDSOUND.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 106 and music stopped")
end)


-- Dance 107 (Laugh)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Laugh"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("VOIDSOUND.mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/VOIDSOUND.mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 99222995480853)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("VOIDSOUND.mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 107 and music stopped")
end)















-- Dance 108 (Calmest Crossroads)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Calmest Crossroads"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 88293716395349)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 108 and music stopped")
end)


-- Dance 109 (Pleas)
local player = game.Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

-- Create the tool
local tool = Instance.new("Tool")
tool.Name = "Pleas"
tool.RequiresHandle = false
tool.Parent = backpack

        writefile("anybodycanfindlove(except you.).mp3", game:HttpGet("https://github.com/EpicFire8/Music/raw/refs/heads/main/anybodycanfindlove(except you.).mp3"))
-- Load Animator if not already loaded
if not getgenv()["Animator"] then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xhayper/Animator/main/Source/Main.lua"))()
    end

local Anim = nil
local sound = nil

tool.Equipped:Connect(function()
    local character = player.Character
    if character then
        Anim = Animator.new(character, 79550207601451)
        Anim:Play()
        Anim.Stopped:Connect(function()
            Anim:Play()
        end)
        sound = Instance.new("Sound")
        sound.SoundId = getcustomasset("anybodycanfindlove(except you.).mp3")
        sound.Volume = 2
        sound.Looped = true
        sound.Parent = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
        sound:Play()
    end
end)

tool.Unequipped:Connect(function()
    if Anim then
        Anim:Stop()
        Anim:Destroy()
    end
    if sound then
        sound:Stop()
        sound:Destroy()
    end
    print("Dance 109 and music stopped")
end)









